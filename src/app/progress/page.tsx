'use client';

import React from 'react';
import { PlaceholderView } from '@/components/shared/PlaceholderView';

export default function ProgressPage() {
  return (
    <PlaceholderView
      title="Progress & Analytics"
      description="Transparent topic mastery heatmaps, speed vs accuracy scatter plots, syllabus completion radar, and transparent AIR-1 readiness trajectory score breakdown."
      phaseLabel="Phase 8 Progress Analytics Surface"
    />
  );
}
